<% provide :title, t('.title') %>

<p class="breadcrumb"><%= link_to t('.home_link'), root_path %> > <%= link_to t('.events_link'), events_path %> > <%= link_to @event.name, event_path(@event) %> > <%= t('.application') %></p>

<h1 class="page-title"><span class="page-title-firstword"><%= t('.application_for') %></span> <%= @event.name %></h1>

<%= render partial: "events/application_details" %>

<%= render partial: "form" %>
